<div class="main-navbar navbar navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button class="navbar-toggle" type="button" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Cartoque', root_path, class: 'navbar-brand' %>
    </div>
    <div class="navbar-collapse">
      <ul class="nav navbar-nav">
      <% if signed_in? %>
        <li><%= link_to 'Applications', apps_path %></li>
        <li><%= link_to 'Servers', servers_path %></li>
        <li><%= link_to 'Contacts', contacts_path %></li>
      <% end %>
      </ul>
      <% if signed_in? %>
      <ul class="nav navbar-nav pull-right">
        <li><%= link_to content_tag(:i, '', class: 'fa fa-cog'), users_path %></li>
      </ul>
      <% end %>
    </div>
  </div>
</div>
