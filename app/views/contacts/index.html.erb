<h1>Contacts</h1>

<table class="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan=2 class=action-buttons>
        <%= link_to 'New Contact', new_contact_path, id: 'new' %>
      </th>
    </tr>
  </thead>

  <tbody>
    <% @contacts.each do |contact| %>
      <tr>
        <td><%= link_to contact.name, contact %></td>
        <td class=action-buttons><%= link_to 'Edit', edit_contact_path(contact), class: 'edit' %></td>
        <td class=action-buttons><%= link_to 'Destroy', contact, method: :delete, data: { confirm: 'Are you sure?' }, class: 'delete' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= will_paginate @contacts, previous_label: h("<"), next_label: h(">") %>
