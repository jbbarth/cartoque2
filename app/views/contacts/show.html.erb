<h1><%= link_to "Contacts", contacts_path %></h1>

<div class=show>
  <div class=pull-right>
    <%= link_to 'Edit', edit_contact_path(@contact), id: 'edit' %>
  </div>
  <h2><%= @contact.name %></h2>
</div>
